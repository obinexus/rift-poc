# Makefile configuration extracted from .riftrc
# This file is automatically included by Makefile

# Paths from .riftrc
SOURCE_ROOT = src
INCLUDE_ROOT = include
PROJECT_ROOT = include/project-root
OUTPUT_ROOT = out
ARTIFACTS_ROOT = out/artifacts
EXAMPLES_ROOT = examples

# Compilation settings
STAGE0_ENABLED = true
GOVERNANCE_ENABLED = true
VERBOSE = true

# Output patterns
RIFT_SOURCE_PATTERN = *.rift
RIFT0_PATTERN = *.rift.0
RIFT1_PATTERN = *.rift.1
GOV_PATTERN = *.rift.gov
